# === Base ===
PROJECT_ID=your-gcp-project-id
LOCATION=europe-central2
MODEL_NAME=gemini-2.5-flash

# DB backend: firestore | sql
DB_BACKEND=firestore

# Set true to use a mock (local) LLM if Vertex AI (Google Gen AI) is unavailable
USE_VERTEX_MOCK=false

# === Optional SQL (Pro-block) ===
# Example for Cloud SQL via Unix socket (pg8000 driver)
# DB_URL=postgresql+pg8000://postgres:<PASSWORD>@/chatdb?unix_sock=/cloudsql/<PROJECT>:<REGION>:<INSTANCE>/.s.PGSQL.5432
# Or regular TCP (not recommended on Cloud Run without proper networking):
# DB_URL=postgresql+pg8000://user:password@host:5432/chatdb
